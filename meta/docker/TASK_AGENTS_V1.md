# AGENTS-guided Smoke V1

You are in a cold-start local environment.

Available context:

- A `cowork` install script URL
- A managed knowledge repository URL
- Global AGENTS guidance already configured in the environment

Goal:

1. Set up the local knowledge repository through the intended `cowork` path.
2. Use that path to understand repository boundary and collaboration flow.
3. Make one minimal, low-risk documentation-only improvement.
4. Create a draft PR with a concise why-focused summary.

Constraints:

- Prefer the intended coordination path over ad-hoc cloning.
- Do not make structural refactors.
- Do not delete content.
- Keep the change reversible and low risk.
- Treat `preview`, `contribute`, and `resource` as guide-only commands.
