# cowork-v0.1.4

- Convert `cowork clone preview` into a guide-only command that prints actionable metadata instead of starting tmux automatically.
- Align `preview`, `contribute`, and `resource` around the same guide-only trust-agent semantics.
- Publish the first beta-capable release workflow with matching tag conventions.
